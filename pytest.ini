[pytest]
addopts = -ra --strict-markers
testpaths = tests
markers =
    unit: Fast feedback tests for isolated helpers and pure logic.
    integration: API and module boundary tests with mocked external systems.
    component: In-process component and cross-component interaction tests.
    e2e: End-to-end smoke tests for deployment rendering and catalog integrity.
